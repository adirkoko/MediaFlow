# MediaFlow

MediaFlow includes:
- `backend/`: FastAPI service for auth, jobs, download processing, and usage tracking.
- `frontend/`: static web UI (served by nginx in Docker).

For production on Ubuntu, this repo is designed to run with a single compose stack:
- [docker-compose.yml](./docker-compose.yml)
- [.env.production.example](./.env.production.example)
- [DEPLOY.md](./DEPLOY.md)

## Production (Ubuntu Server)

1. Copy env template:
```bash
cp .env.production.example .env
```

2. Set required values in `.env`:
- `MEDIAFLOW_JWT_SECRET`
- `MEDIAFLOW_CORS_ORIGINS`
- `HOST_MEDIAFLOW_DATA_ROOT`

3. Start stack:
```bash
docker compose --env-file .env up -d --build
```

4. Verify:
```bash
docker compose ps
curl http://127.0.0.1:18080/health
```

Frontend will be available on `http://<server-ip>:8080` (default).

## Persistent Data Layout

Recommended homelab structure:
- `/srv/services/mediaflow` -> repository + compose files
- `/srv/data/mediaflow` -> persistent data outside the repo

Compose maps host paths to container paths:
- `/srv/data/mediaflow/backend-data` -> `/var/lib/mediaflow/data`
- `/srv/data/mediaflow/outputs` -> `/var/lib/mediaflow/outputs`
- `/srv/data/mediaflow/secrets` -> `/var/lib/mediaflow/secrets` (read-only)

## Frontend API Configuration

Frontend API base is dynamic at runtime via:
- [frontend/js/runtime-config.js](./frontend/js/runtime-config.js)

In Docker, this file is generated by:
- [frontend/docker-entrypoint.d/40-runtime-config.sh](./frontend/docker-entrypoint.d/40-runtime-config.sh)

Default production value is `MEDIAFLOW_API_BASE=/api`, and nginx proxies `/api/*` to the backend service.

## Backend Development Docs

For backend API details and local (non-Docker) development, see:
- [backend/README.md](./backend/README.md)
